/*
import Vue from 'vue'
import VueRouter from 'vue-router'

// 引入组件
import news from '../components/router-example/news'
import message from '../components/router-example/message'

// 使用路由VueRouter
Vue.use(VueRouter)

const routers = [
  {
    path: '/news',
    component: news
  },
  {
    path: '/message',
    component: message
  }
]

var nestedRouter = new VueRouter({
  routes: routers
})

export default nestedRouter
*/
